# Loop progress — {{TOPIC}}

> Append-only. One line per checkpoint. The status script tails this every tick;
> the user reads the "Final state" section cold when they return.
>
> Format: `- YYYY-MM-DD HH:MM — <what shipped or happened> — <link or ref>`

## Progress

- {{START_TIMESTAMP}} — loop initialised by `unattended-loop-session` skill — brief at `.claude/loop-brief.md`

## Final state

(written on the last tick before the loop ends — replace this placeholder)
