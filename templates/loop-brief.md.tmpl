# Session brief — {{TOPIC}}

> The `loop.md` directive (rules + recipe) is fixed. **This file is what changes.**
> Edit any section between ticks to redirect the loop. The next fire reads it fresh.

## Goal

{{GOAL_PARAGRAPH}}

## Track

- Plan: `{{PLAN_PATH}}`
- Design doc: `{{DESIGN_PATH}}`
- Umbrella issue: {{UMBRELLA_ISSUE}}
- Affected repos: {{AFFECTED_REPOS}}

## Definition of done

{{DEFINITION_OF_DONE}}

## Scope of autonomy

Default rules in `loop.md` are in force. Session-specific carve-outs below.

### Playbook overrides

{{PLAYBOOK_OVERRIDES}}

### Off-limits without user input

> Use this for **both** gated decisions (e.g. "anything that touches production credentials — PushNotification before acting") **and** paths/modules to leave alone (e.g. "do not touch `modules/website` — separate refactor in flight"). The loop reads this section every tick before acting.

{{OFF_LIMITS}}

### Pre-approved beyond defaults

{{PRE_APPROVED}}

## Notes for future-you (post-compaction)

{{NOTES}}
